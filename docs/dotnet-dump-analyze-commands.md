# Documentation for the output from the dotnet dump analyze commands

While this project uses ClrMD (nuget package Microsoft.Diagnostics.Runtime), we have made an effort to show how the command output looks when using the standard `dotnet dump analye` CLI command.
